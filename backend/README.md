# Instructions
1. Copy `env.example` and rename it to `.env`
2. Edit the environment variable if needed
3. Start MySQL service either through local or docker
4. Run `database/pokemon_db.sql` in the MySQL terminal
5. Run `npm start dev` to run the backend server in development
